# For local scraper runs and dev
lbc>=1.0.5
requests>=2.28.0
geopy>=2.4.0
beautifulsoup4>=4.12.0
ebaysdk-python>=3.0.0
# For scripts/run_local_scrapers.py (upload to Databricks Volume)
databricks-sdk>=0.20.0
# Competition scraper (Vinted, La Redoute): reduce 403 with Chrome impersonation
curl_cffi>=0.6.0
# Competition scraper (Beebs, La Redoute): optional browser fallback (pip install playwright && playwright install chromium)
playwright>=1.40.0
